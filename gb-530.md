# gb-530 辐射作业个人剂量预算

请生成英文短名为 `radiation-dose-budget-control` 的 Go 全栈 Web 项目。系统面向辐射防护计划人员，用离线数据维护人员授权概况、作业计划和已确认暴露记录，计算期间累计量、剩余预算及计划情景的比较结果。它不是剂量计、医疗系统或作业许可控制器，不采集实时设备数据、不自动批准作业，任何结果仅供 ALARA 规划和人工复核，不能替代法规、辐射防护负责人或医疗意见。项目不含排班预约、工单、财务、计费、库存或人员绩效功能。

## 项目主要需求

### 1. 核心实体

- `WorkerProfile`：字段至少含 `id`、`worker_code`、`display_name`、`authorization_level`、`annual_limit_msv`、`administrative_limit_msv`、`profile_status`、`period_start`、`version`；最小化保存计划所需信息，不存诊断记录，贯穿全栈。
- `WorkPermitPlan`：字段至少含 `id`、`plan_code`、`worker_id`、`work_area`、`task_category`、`estimated_rate_msvh`、`planned_minutes`、`controls_json`、`permit_status`、`version`、`reviewer_id`；仅是规划草案，贯穿全栈。
- `ExposureEntry`：字段至少含 `id`、`worker_id`、`source_ref`、`occurred_at`、`dose_msv`、`entry_type`、`quality_flag`、`verified_by`、`correction_of_id`、`note`；更正用反向/替代记录，禁止覆盖原值，贯穿全栈。
- `DoseBudgetAssessment`：字段至少含 `id`、`worker_id`、`plan_id`、`assessment_status`、`input_snapshot_json`、`period_dose_msv`、`projected_dose_msv`、`remaining_admin_msv`、`remaining_legal_msv`、`risk_band`、`evidence_json`、`created_at`、`reviewed_at`；结果不可覆盖并可重放，贯穿全栈。

### 2. 核心页面

- `/workers` 授权概况页：消费 `WorkerProfile`、`ExposureEntry`，查看最小资料和期间累计值。
- `/plans` 作业计划页：消费 `WorkPermitPlan`、`WorkerProfile`，编辑剂量率/时长假设和控制措施。
- `/exposures` 暴露记录页：消费 `ExposureEntry`、`WorkerProfile`，录入、核验和更正离线记录。
- `/budgets` 剂量预算评估页：消费 `DoseBudgetAssessment`、`WorkPermitPlan`、`ExposureEntry`，运行情景并解释预算证据。
- `/audit` 审计复核页：消费 `DoseBudgetAssessment`、`WorkPermitPlan`，提交复核、比较快照和查看操作记录。

### 3. 差异算法与状态流

- 按配置化统计周期汇总经核验的 `ExposureEntry`，正确处理更正链、边界时间、重复 source_ref 和质量标记；原始记录与更正均保留。
- 计划投影为 `estimated_rate_msvh * planned_minutes / 60`，叠加当前期间剂量，分别计算行政控制余量与法规限值余量；所有单位统一为 mSv 并防止负数/NaN。
- 提供多计划情景比较与时间加权投影，输出输入证据、公式、阈值版本和风险带。不得返回“允许作业”或自动许可，超阈值仅标记必须人工升级复核。
- `WorkPermitPlan` 状态为 `draft -> assessed -> pending_rpo_review -> planning_accepted | rejected -> archived`；`planning_accepted` 明确不等于现场许可，仅 RPO reviewer 可操作，事务和乐观锁防并发越级。

### 4. 横切关注点

- JWT/RBAC 角色 `planner`、`rpo_reviewer`、`admin`，贯穿角色数据、认证/授权中间件、后端路由、前端守卫/按钮与类型。
- 审计覆盖限值、计划假设、暴露录入/更正、评估快照、状态和复核，包含 request ID、操作者和前后摘要，普通 API 不可删除。
- 全局错误处理统一单位/范围、重复来源、更正链冲突、状态冲突、版本冲突和无权限；前端不得误显示批准。
- 请求追踪、结构化日志、panic recovery、本地限流独立实现；日志对人员信息最小化并不记录 JWT。

### 5. 共享枚举、组件、hooks 与中间件

- `PermitStatus = draft | assessed | pending_rpo_review | planning_accepted | rejected | archived`：后端 `backend/internal/constants/permit.go`，前端 `frontend/src/types/permit.ts`。
- `DoseBand = within_admin | above_admin | near_legal | above_legal | invalid`：后端 `backend/internal/constants/dose.go`，前端 `frontend/src/types/dose.ts`。
- README 列出枚举在数据库、后端 model/dto/service/handler 和前端 store/component/page 的全部位置。
- 共享组件：`DoseBandBadge`（人员/预算页）、`BudgetEvidencePanel`（预算/审计页）、`SafetyBoundaryBanner`（计划/预算/人员页）。
- hooks：`useAuth`、`useBudgetAssessment`，集中处理权限、提交和轮询。
- 中间件至少独立实现 `RequestID`、`AccessLog`、`Auth`、`RBAC`、`Recovery`、`RateLimit`。

### 6. 技术与规模红线

- Angular 17 + TypeScript + Angular CLI/esbuild + Angular Material + RxJS，剂量图表与预算证据使用真实 API 数据。
- Go 1.22 + Gin + GORM + PostgreSQL，validator/v10、结构化日志、构造器注入、事务、`%w` 和优雅停机。
- 功能 Go 代码 2500-4200 行、24-42 个功能 `.go` 文件且低于 5000 行，不计测试/vendor/生成代码/前端；禁止空壳或职责合并。
- API `/api/v1`，真实 `/healthz`、`/readyz`、统一响应/分页/错误码；周期边界、更正链、单位换算、投影和状态机必须有表驱动测试。
- 禁止 TODO、placeholder、随机数据冒充剂量、自动作业许可、医疗建议、设备接入或 handler 直连数据库。

### 7. 文件结构强制清单

```text
./
├── go.work                 # use ./backend
├── docker-compose.yml
├── .env
├── .env.example
├── .gitignore
├── README.md
├── runtime_smoke.json
├── backend/
│   ├── go.mod
│   ├── Dockerfile
│   ├── cmd/server/main.go
│   └── internal/{config,model,dto,repository,service,handler,router,middleware,constants,dosebudget,util}/
├── frontend/
│   ├── Dockerfile
│   ├── nginx.conf
│   └── src/app/{api,stores,types,components/common,hooks,pages,router,utils}/
└── output/execution.md
```

实体逐层分文件；周期汇总、更正链、投影、阈值判定各自分文件。严禁合并职责到单一文件。

根目录 `runtime_smoke.json` 只能是服务启动 manifest，构建/API/Browser/截图证据只能写入 `output/execution.md`。必须提交以下可解析内容；后端须支持 smoke 环境的 GORM SQLite 和 Compose 环境的 PostgreSQL：

```json
{
  "mode": "service",
  "start": "go run ./cmd/server",
  "workdir": "backend",
  "env": {
    "PORT": "20530",
    "DB_DRIVER": "sqlite",
    "DB_DSN": "file:runtime-smoke?mode=memory&cache=shared",
    "DB_AUTO_MIGRATE": "true",
    "JWT_SECRET": "runtime-smoke-gb530-jwt-secret-at-least-32-bytes"
  },
  "ready_url": "http://127.0.0.1:20530/healthz",
  "ready_status": [200],
  "startup_timeout": 30
}
```

### 8. 部署、README 与端口

- Compose 名 `radiation-dose-budget-control`；前端 `18530`、后端 `19530`、PostgreSQL `57530`，容器内 `80/8080/5432`。
- Compose 顶层 `name` 且无 `version:`，容器名带 `${COMPOSE_PROJECT_NAME:-radiation-dose-budget-control}`；数据库使用命名卷，数据库、后端和前端均配置 healthcheck 并按健康状态依赖，`docker compose ps` 中所有长驻服务最终必须为 healthy。
- `.env`/示例含 Compose 名、端口、数据库、JWT、CORS、默认周期/阈值和日志级别。Nginx `/api` 反代与 SPA fallback；中文目录一键启动。
- README 中文且 Docker 快速启动优先，包含复制 env、地址、测试账号、功能、技术栈、目录、变量、API、枚举位置、公式/单位/阈值、安全与隐私边界、开发命令、排错和 License。

### 9. 强制真实验收

1. 根目录执行 `go work sync`、`go build ./backend/...`、`go vet ./backend/...`、`go test ./backend/...`；规模实测必须为 2500-4200 行、24-42 个功能 `.go` 文件且低于 5000 行；前端完成依赖安装、类型检查和 `npm run build` 生产构建。
2. 执行 Compose config、构建启动、ps、健康/就绪检查，确认数据库/后端 healthy、前端可访问。
3. 至少 8 项接口冒烟：登录、人员档案创建、暴露记录创建、重复来源拒绝、更正记录、计划创建、预算评估、阈值升级、提交 RPO 复核、越级拒绝和审计查询；真实结果只写入 `output/execution.md`，不得写入 `runtime_smoke.json`。
4. 只用 Codex 内置 Browser 验证登录、5 页面、期间累计、计划编辑、预算证据、安全提示和 RPO 权限；禁止外部 Chrome，截图文件一律保存到 `output/`，Browser 步骤与结论只写入 `output/execution.md`。
5. 执行 `docker compose down -v --remove-orphans` 并确认本项目无容器残留，不影响其他项目。
6. 项目目录独立执行 `git init`，设置仓库本地身份 `git config user.name "blueship581"`、`git config user.email "brysj.hhrhl.g@gmail.com"`。先提交除 `output/` 验证证据外的实现文件并取得实现 commit hash，将 hash 写入 `output/execution.md`，再把 `output/execution.md` 与截图作为第二次报告提交；最终 `git status --short` 为空。
